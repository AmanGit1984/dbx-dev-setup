# This is a Databricks asset bundle definition for dbx_dev_setup.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_dev_setup
  uuid: f668b456-1387-403f-9228-1d708ad422a0

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-5178312734915975.15.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-5178312734915975.15.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/amandeep.rai@brewin.co.uk to make sure we only have a single copy.
      root_path: /Workspace/Users/amandeep.rai@brewin.co.uk/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: amandeep.rai@brewin.co.uk
        level: CAN_MANAGE
